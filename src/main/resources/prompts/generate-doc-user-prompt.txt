请围绕以下主题生成完整 Markdown 文档。

【标题】
{{title}}

【用户原始输入】
{{prompt}}

【输入信息量】
{{promptDetailLevel}}

【生成策略】
1. 如果输入信息量为 BRIEF：
   - 用户通常只给了主题或一句话。
   - 你必须主动补充合理结构、必要背景、典型内容和具体例子。
   - 不要因为用户输入短就输出短文。
   - 生成不少于 1200 字的完整 Markdown 文档。

2. 如果输入信息量为 NORMAL：
   - 用户已经给出部分要求。
   - 必须保留用户指定主题、范围、章节和交付形式。
   - 在用户要求基础上补充背景、例子、解释和过渡。
   - 生成不少于 1000 字的完整 Markdown 文档。

3. 如果输入信息量为 DETAILED：
   - 用户已经提供较多事实、材料或章节。
   - 必须以用户提供内容为主要依据。
   - 不要大幅引入无关新内容。
   - 可以做结构优化、语言润色、逻辑补充和适度扩展。
   - 不要覆盖、删减或扭曲用户提供的关键信息。
   - 输出长度应不低于用户输入长度，且不少于 800 字。

请直接输出成品 Markdown。
