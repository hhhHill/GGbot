/**
 * `planner` 包负责把用户输入转换成可执行的内部计划。
 *
 * <p>这一层不直接执行工具，也不关心 Feishu 或 Web 等接入渠道，而是专注于两件事：
 *
 * <ul>
 *   <li>识别当前请求更适合走哪种意图，例如聊天、生成文档、生成 PPT 或两者同时生成</li>
 *   <li>把识别结果组织成 `Plan` 与 `PlanStep`，交给上层 `AgentService` 继续执行</li>
 * </ul>
 *
 * <p>当前实现采用“规则匹配 -> 规划信号 -> 执行步骤”的结构，后续如果需要接入 LLM 规划器，
 * 可以在不破坏上层调用方式的前提下继续扩展。
 */
package org.example.ggbot.planner;
